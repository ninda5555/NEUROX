# Credentials & tokens — never committed (CLAUDE.md §12)
config.yaml
.tokens/

# Data & model artifacts (regenerable, large) — anchored to repo root only;
# unanchored "data/" previously also matched src/data/ (the CandleStore
# package) and silently excluded it from every commit (found 12-Jul-2026).
/data/
/models/

# Python
__pycache__/
*.py[cod]
*.egg-info/
.pytest_cache/
.venv/
venv/

# Logs
*.log
fyers_logs/

# OS / editor
.DS_Store
